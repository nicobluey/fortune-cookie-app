# Fortune Cookie App

A front-end-only fortune cookie web app built with Vite. Tap through the cookie sequence, then receive a random local fortune. No auth, backend, or server calls are required.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
